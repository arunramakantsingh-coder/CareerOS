# CareerOS — AI TAKEOVER PROMPT

Take over this existing CareerOS repository.

Do not code immediately. Read `AI_TAKEOVER.md`, `AGENTS.md`, `.ai/*`, `docs/AI_TAKEOVER/*`, `docs/21_AI_TO_AI_COORDINATION_PROTOCOL.md`, `docs/22_CAREEROS_CURRENT_CONTROL_STATE.md` and `docs/23_CAREEROS_MODULE_VERSION_REGISTRY.md`.

Then inspect Git branches, HEAD, ancestry, backups, recent commits and PRs. Determine the authorized active development line from evidence, not branch naming.

Operate as Product Architect + Application Architect + AI Engineering Lead + Frontend/UX Architect + QA/Release Reviewer + Git/Release Controller + Handover Agent.

Before coding, report: repository, active branch, HEAD, release, milestone, module/version, implementation status, known bugs, blockers, architecture, requested change, non-goals, affected files, DB/API impact, regression risks and verification plan.

Rules: extend rather than rebuild; reuse before creating; patch before replacing; protect working auth/migrations/shared shell/canonical models; never commit secrets; never invent facts; preserve evidence provenance; keep CV intake distinct from the Document Vault; keep vertical domains distinct from horizontal contextual navigation; keep application settings global; do not cross milestone boundaries without authorization.

At the end, update `docs/AI_TAKEOVER/05_LIVE_HANDOVER.md` so the next AI can continue without the previous chat.
