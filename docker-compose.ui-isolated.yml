services:
  postgres:
    ports:
      - "5433:5432"

  backend:
    ports:
      - "8100:8000"

  frontend:
    ports:
      - "3100:3000"
    environment:
      NEXT_PUBLIC_API_URL: "http://localhost:8100"
