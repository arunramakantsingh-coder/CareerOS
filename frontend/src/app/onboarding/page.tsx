 'use client';
import {useEffect,useState} from 'react'; import Link from 'next/link'; import {CareerOSShell,Card,PageHeader,Badge,Button} from '@/components/CareerOSShell'; import {apiClient} from '@/lib/api/client';
export default function Onboarding(){const [profile,setProfile]=useState<any>(null);const [msg,setMsg]=useState('');useEffect(()=>{apiClient.profile().then(x=>setProfile(x.profile)).catch(()=>{})},[]);const complete=!!profile;return <CareerOSShell><PageHeader eyebrow="Getting started" title="Build your CareerOS foundation" description="Complete the minimum foundation before relying on matching, application generation and interview intelligence." action={<Badge tone={complete?'good':'warn'}>{complete?'Foundation ready':'Foundation incomplete'}</Badge>}/><div className="grid gap-4 md:grid-cols-2"><Card><Badge tone="blue">1 · Career Vault</Badge><h2 className="mt-3 text-lg font-semibold">Create your source of truth</h2><p className="mt-2 text-sm text-muted-foreground">Add your role targets, experience, preferences and verified evidence.</p><Link className="mt-5 inline-block text-sm font-semibold text-primary" href="/career-vault">Open Career Vault →</Link></Card><Card><Badge tone="blue">2 · Persona</Badge><h2 className="mt-3 text-lg font-semibold">Define your positioning</h2><p className="mt-2 text-sm text-muted-foreground">Create a persona based on the Career Vault profile.</p><Link className="mt-5 inline-block text-sm font-semibold text-primary" href="/personas">Open Personas →</Link></Card><Card><Badge tone="blue">3 · Job DNA</Badge><h2 className="mt-3 text-lg font-semibold">Import a target job</h2><p className="mt-2 text-sm text-muted-foreground">Paste a real job description and analyze its requirements.</p><Link className="mt-5 inline-block text-sm font-semibold text-primary" href="/jobs">Open Jobs →</Link></Card><Card><Badge tone="blue">4 · Interview</Badge><h2 className="mt-3 text-lg font-semibold">Prepare from evidence</h2><p className="mt-2 text-sm text-muted-foreground">Create an interview plan after creating an application.</p><Link className="mt-5 inline-block text-sm font-semibold text-primary" href="/interviews">Open Interviews →</Link></Card></div>{msg&&<p className="mt-5 text-sm text-muted-foreground">{msg}</p>}<div className="mt-6"><Button onClick={()=>window.location.href='/'}>Go to Dashboard</Button></div></CareerOSShell>}
