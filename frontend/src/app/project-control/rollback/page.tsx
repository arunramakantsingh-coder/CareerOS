import Link from 'next/link';
import { CareerOSShell, PageHeader, Card, Badge } from '@/components/CareerOSShell';
export default function RollbackRecovery(){return <CareerOSShell><PageHeader eyebrow="Project Control" title="Rollback & Recovery" description="Recovery is deliberate: compare known Git points, assess impact, then perform any repository rollback explicitly outside the application." action={<Badge tone="warn">Explicit confirmation</Badge>}/><div className="grid gap-4 lg:grid-cols-3"><Card title="Current rule"><p className="text-sm leading-6 text-muted-foreground">Never silently delete commits or reset the database. Preserve working functionality and use known Git recovery points.</p></Card><Card title="Compare"><p className="text-sm leading-6 text-muted-foreground">Use GitHub compare and pull-request history to inspect the exact changes between baselines before recovery.</p></Card><Card title="Recover"><p className="text-sm leading-6 text-muted-foreground">Repository-level rollback remains a controlled Git operation. CareerOS does not rewrite Git history.</p><Link href="/project-control/version" className="mt-4 inline-block text-sm font-semibold text-primary">Review version history →</Link></Card></div></CareerOSShell>}
