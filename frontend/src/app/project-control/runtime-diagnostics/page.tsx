'use client';
import { useEffect, useState } from 'react';
import { CareerOSShell, PageHeader, Card, Badge } from '@/components/CareerOSShell';
import { apiClient } from '@/lib/api/client';
export default function RuntimeDiagnostics(){const [data,setData]=useState<any>();const [error,setError]=useState('');useEffect(()=>{apiClient.get('/api/v1/developer/diagnostics').then(setData).catch((e:any)=>setError(e.message||'Unable to load diagnostics'));},[]);return <CareerOSShell><PageHeader eyebrow="Project Control" title="Runtime Diagnostics" description="Safe runtime metadata for the current CareerOS development environment." action={<Badge tone={data?'good':'warn'}>{data?'Available':'Checking'}</Badge>}/><div className="grid gap-4 md:grid-cols-2 lg:grid-cols-4">{[['Version',data?.version],['Environment',data?.environment],['Git branch',data?.git_branch],['Git commit',data?.git_commit]].map(([label,value])=><Card key={label as string}><p className="text-xs text-muted-foreground">{label}</p><p className="mt-2 break-all font-semibold">{value||'—'}</p></Card>)}</div>{error&&<div className="mt-4 rounded-xl border bg-card px-4 py-3 text-sm">{error}</div>}</CareerOSShell>}
