'use client';
import { useState } from 'react';
import { CareerOSShell, PageHeader, Card, Button, Badge } from '@/components/CareerOSShell';
import { apiClient } from '@/lib/api/client';
const options=[['career_data','Career data','Clear parsed profile facts and evidence links.'],['documents','Documents','Remove uploaded test documents.'],['personas','Personas','Clear generated persona data.'],['connections','Connections','Clear connector records and Gmail mailbox tokens while preserving sign-in.'],['all','Everything','Return the developer account to a clean onboarding test state.']];
export default function TestData(){const [scope,setScope]=useState('all');const [msg,setMsg]=useState('');const [busy,setBusy]=useState(false);const reset=async()=>{if(!confirm(`Reset ${scope} test data? Your login account will be preserved.`))return;setBusy(true);setMsg('');try{const r=await apiClient.post<any>('/api/v1/developer/reset',{scope});setMsg(`Reset complete: ${r.scope}. Login preserved.`);}catch(e:any){setMsg(e.message||'Reset failed');}finally{setBusy(false);}};return <CareerOSShell><PageHeader eyebrow="Project Control" title="Test Data & Reset" description="Safe developer-only controls for resetting test state without deleting the login account." action={<Badge tone="warn">Destructive controls</Badge>}/><Card title="Reset test data"><div className="grid gap-3 md:grid-cols-2 lg:grid-cols-3">{options.map(([key,title,description])=><button key={key} type="button" onClick={()=>setScope(key)} className={`rounded-xl border p-4 text-left transition ${scope===key?'border-primary bg-primary/10':'hover:border-primary/40 hover:bg-muted/30'}`}><p className="font-semibold">{title}</p><p className="mt-1 text-xs leading-5 text-muted-foreground">{description}</p></button>)}</div><div className="mt-5 flex flex-wrap items-center gap-3"><Button onClick={reset} disabled={busy}>{busy?'Resetting…':'Reset selected test data'}</Button><span className="text-xs text-muted-foreground">Selected: {scope}</span></div></Card>{msg&&<div className="mt-4 rounded-xl border bg-card px-4 py-3 text-sm">{msg}</div>}</CareerOSShell>}
