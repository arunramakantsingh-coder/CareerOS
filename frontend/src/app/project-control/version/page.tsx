'use client';
import { useEffect, useState } from 'react';
import { CareerOSShell, PageHeader, Card, Badge } from '@/components/CareerOSShell';
import { apiClient } from '@/lib/api/client';
export default function VersionHistory(){const [data,setData]=useState<any>();useEffect(()=>{apiClient.get('/api/v1/developer/diagnostics').then(setData).catch(()=>{});},[]);return <CareerOSShell><PageHeader eyebrow="Project Control" title="Version & Git History" description="The current code/version source of truth is GitHub. CareerOS surfaces safe metadata here without rewriting Git history." action={<Badge tone="good">Git-backed</Badge>}/><div className="grid gap-4 md:grid-cols-2 lg:grid-cols-4"><Card><p className="text-xs text-muted-foreground">Current branch</p><p className="mt-2 break-all font-semibold">{data?.git_branch||'—'}</p></Card><Card><p className="text-xs text-muted-foreground">Current commit</p><p className="mt-2 break-all font-mono text-sm">{data?.git_commit||'—'}</p></Card><Card><p className="text-xs text-muted-foreground">Latest baseline</p><p className="mt-2 font-semibold">Use Project Tracker recovery points</p></Card><Card><p className="text-xs text-muted-foreground">Repository</p><a className="mt-2 block break-all font-semibold text-primary" href={data?.github_repository||'https://github.com/arunramakantsingh-coder/CareerOS'} target="_blank" rel="noreferrer">CareerOS on GitHub →</a></Card></div></CareerOSShell>}
