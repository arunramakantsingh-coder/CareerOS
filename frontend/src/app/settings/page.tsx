 'use client';
import {useEffect,useState} from 'react'; import {CareerOSShell,Card,PageHeader,Button,Badge} from '@/components/CareerOSShell'; import {apiClient} from '@/lib/api/client';
export default function Settings(){const [me,setMe]=useState<any>();const [health,setHealth]=useState<any>();const [msg,setMsg]=useState('');useEffect(()=>{Promise.all([apiClient.me(),apiClient.healthCheck()]).then(([u,h])=>{setMe(u);setHealth(h)}).catch(e=>setMsg(e.message))},[]);return <CareerOSShell><PageHeader eyebrow="System" title="Settings" description="Manage your CareerOS session, workspace identity and local development status."/><Card title="Current session"><div className="grid gap-5 md:grid-cols-3"><div><p className="text-xs text-muted-foreground">Identity</p><p className="mt-1 font-medium">{me?.name||me?.email||'—'}</p></div><div><p className="text-xs text-muted-foreground">Tenant</p><p className="mt-1 font-mono text-sm">{me?.tenant_id||'—'}</p></div><div><p className="text-xs text-muted-foreground">Authentication</p><Badge tone="good">Authenticated</Badge></div></div></Card><div className="mt-4 grid gap-4 md:grid-cols-2"><Card title="Account & profile"><p className="text-sm text-muted-foreground">Your identity and career data are bound to your authenticated tenant.</p></Card><Card title="Security & access"><p className="text-sm text-muted-foreground">JWT authentication is enabled for the v0.1 workflow.</p></Card><Card title="API health"><p className="text-sm text-muted-foreground">Backend: <strong>{health?.status||'checking…'}</strong></p></Card><Card title="Data privacy"><p className="text-sm text-muted-foreground">CareerOS v0.1 keeps generated career claims grounded in the evidence stored in your workspace.</p></Card></div>{msg&&<p className="mt-4 text-sm text-red-600">{msg}</p>}</CareerOSShell>}
